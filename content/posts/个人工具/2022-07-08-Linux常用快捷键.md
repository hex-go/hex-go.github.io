---
title: Linux常用快捷键
categories:
  - 个人工具
tags:
  - 个人工具
date: '2022-07-08 02:47:38'
top: false
comments: true
---

# 重要
最重要的事: 

# 1. 命令行快捷键
> 参见 `man readline` 来查看 Bash 中所有默认的键盘绑定, 可以使用 `set -o vi` 使用 `vi` 风格的键盘绑定。 

> 命令行编辑：光标移动
##　1.1 行首、行尾
`Ctrl` + `a`: 转到行的开头。 == `Home键` == `alt` + `shift` + `b`

`Ctrl` + `e`: 转到行的结尾。 == `End键` == `alt` + `shift` + `f`

##　1.2 前移、后移 1字符
`Ctrl` + `b`: 左移动1个字符。 == 方向左键`<-`
`Ctrl` + `f`: 右移动1个字符。 == 方向右键`->`

##　1.3 前移、后移 1单词
`Ctrl` + 方向左键`<-`: 左移动1个单词。== `alt` + `b`(back)
`Ctrl` + 方向左键`->`: 右移动1个单词。== `alt` + `f`(forward)

> 命令行编辑：快捷删除
## 1.4 删除至行首、删除至行尾
`Ctrl` + `u`删除至行首，`Ctrl + k`删除至行尾。

## 1.5 删除前一项
`Ctrl` + `w`删除删除光标处左边一个单词，或至单词首的内容。

## 1.6 删除后一项
`Alt` + `d`: 删除光标处右边一个单词，或至单词末的内容

## 1.7 删除前一字符
`Ctrl` + `h`: 删除删除光标处左边一个字符。

## 1.8 删除后一字符
`Ctrl` + `d`: 删除光标处右边一个字符。

## 1.9 插入上次删除的内容
`Ctrl` + `y`: 将上一次删除的内容插入到光标处。(光标不移动，等同于撤销操作)。

> 命令行编辑：撤销
## 1.10 撤销修改
`Ctrl`+`shift`+`-`：撤销，即恢复上一次操作，可撤销多次。
注意，这个快捷根据自己系统设定不一样，有的`Ctrl+shift+-`是撤销，`Ctrl+-`是命令行字体变小，而有的系统设置刚好相反。

> 命令行编辑：屏幕调整
## 1.11 字体变小
`Ctrl`+`-`：命令行字体变小，注意看上面撤销修改`Ctrl+shift+-`的说明。

## 1.12 字体变大
`Ctrl`+`shift`+`+`：命令行字体变大。有的系统设置是`Ctrl`+`+`，`ctrl`和`+`的组合是命令行字体变大。

## 1.13 清屏
`Ctrl`+`l`: 清屏，但是当前命令行内容保留

## 1.14 锁定
`Ctrl`+`s`: 锁定命令行

## 1.14 解锁
`Ctrl`+`Q`: 解锁命令行

> 命令行编辑：快捷输入

## 1.15 使用上个命令最后一项
参数: `!$` 或者 快捷键: `alt`+`.`
```bash
ls /home/hex/
```

```bash
cd !$
```
等同于 `cd /home/hex/`

## 1.16 使用上一个命令
参数: `!!`
> 权限不足时 sudo !!
```bash
vi /etc/hosts

sudo !!
```

## 1.17 使用上次删除的内容
`Ctrl` + `y`: 将上一次删除的内容插入到光标处。(光标不移动，等同于撤销操作)。


# 2. vim快捷键

# 3. bash常用命令

## 3.1 文件擦除数据
```bash
> /root/test.md
```
或者
```bash
echo '' > /root/test.md
```

## 3.2 读取日志文件

### 3.2.1 实时读取日志文件
```bash
tail -F linuxidc_log
```

### 3.2.2 less读取文件
```bash
less -N linuxidc.txt
```
//按下v键来编辑文件
//退出编辑器后，可以继续用less浏览
可以在更少的范围内搜索字词，按页移动，高亮与行号等。

###　3.2.3 读取压缩日志而不解压缩
使用zless，zcat，zgrep等z命令查看压缩包的内容，甚至不必显式提取压缩文件
```bash
zcat linuxidc_log.zip | more
```

## 3.3 文件查找

### 3.3.1 查找包含特定文本的文件
> 
```bash
grep -Pri 要搜索的字符串 路径
```

### 3.3.2 精通find命令
[如何使用find命令在Linux中查找文件](https://www.linuxidc.com/Linux/2018-07/153205.htm)

## 3.4 输出查看-表格
```bash
mount |column -t
```

## 3.5 SSH客户端配置优化
`~/.ssh/config` 包含了
- 避免在特定网络环境中连接被断掉的情况的设置      `TCPKeepAlive=yes ServerAliveInterval=15 ServerAliveCountMax=6`
- 使用压缩（这对于通过低带宽连接使用 scp 很有用） `Compression=yes`
- 使用一个本地控制文件来开启到同一台服务器的多通道 `ControlMaster auto   ControlPath /tmp/%r@%h:%p   ControlPersist yes`

```config
TCPKeepAlive=yes
ServerAliveInterval=15
ServerAliveCountMax=6
Compression=yes
ControlMaster auto
ControlPath /tmp/%r@%h:%p
ControlPersist yes
```

## 文件传输
python2

```bash

```

caddy
```bash

```

## 操作不被`history`记录

- 关闭history
```bash
history -c
```

- 操作命令前，加一个`空格`

## 清除指定history记录

```bash
history -d <num>
```

## 进程相关

### 查看运行时间
> 通过 `-o` 参数，指定只显示具体的某个字段，会得到更清晰的结果。
> 通过 `-o etime` 获取该进程的运行时间(etime格式 = %d-%h:%m:%s  etimes格式 = %s)
```bash
ps -p 10167 -o etimes,etime
```

> `-o rss` 可以只获取该进程的内存信息

```bash

```

### 资源使用率排序
- 内存使用率 排序
```bash
ps aux | sort -rnk 4
```

- CPU使用率 排序
```bash
ps aux | sort -nk 3
```

## 网络相关

### 查看本机公网IP

```bash
curl ip.sb
# 或
curl ifconfig.me
```

### 

# Reference

[Linux常用命令行小技巧: 全面+有深度, 有点乱](https://qianngchn.github.io/wiki/7.html)

[80%的人都不会的，15个Linux实用技巧: 清晰，量少](https://www.cnblogs.com/liwei0526vip/p/14624607.html)

[30个高效的Linux命令技巧: Bash语法](https://blog.csdn.net/bbwangj/article/details/82806886)
